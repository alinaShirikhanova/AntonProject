import java.util.Scanner;

public class Lesson10 {
    public static void main(String[] args) {
        // Создаем объект Scanner для чтения ввода пользователя с консоли
        Scanner sc = new Scanner(System.in);

        // Следующие строки закомментированы, но показывают примеры работы со строками:
        // System.out.println(line.length()); // Выводит длину строки line
        // System.out.println(line.charAt(0)); // Выводит первый символ строки line
        // String line = sc.nextLine(); // Читает целую строку из ввода пользователя
        // int lastIndex = line.length() - 1; // Вычисляет индекс последнего символа (длина - 1)
        // System.out.println(line.charAt(lastIndex)); // Выводит последний символ строки

        // Пример сравнения примитивных типов (целых чисел)
        // int a = 12; // Объявляем переменную a типа int и присваиваем значение 12
        // int b = 12; // Объявляем переменную b типа int и присваиваем значение 12
        // System.out.println(a == b); // Сравниваем значения a и b, выводит true если равны

        // Пример сравнения строк (ссылочных типов)
        // String line1 = sc.nextLine(); // Читаем первую строку от пользователя
        // String line2 = sc.nextLine(); // Читаем вторую строку от пользователя
        // System.out.println(line1.equals(line2)); // Сравниваем содержимое строк, а не ссылки

        // Объявляем переменную i типа int и инициализируем значением 1
        // int i = 1;

        // while - цикл "пока" - выполняется до тех пор, пока условие в скобках истинно

        // Цикл while: выполняется пока i меньше 10000
        // while (i < 10000) {
        //     System.out.println(i); // Выводим текущее значение i
        //     i = i + 1; // Увеличиваем i на 1, чтобы в следующий раз условие могло стать ложным
        // }

        // Задача: вывести все числа от 1 до 100, которые делятся на 7 без остатка

        // Объявляем переменную i и инициализируем её 0 - начальное значение для перебора
        // int i = 0;
        // Цикл while выполняется пока i меньше 100
        // while (i < 100){
        //     // Проверяем, делится ли i на 7 без остатка (оператор % возвращает остаток от деления)
        //     if (i % 7 == 0){
        //         System.out.println(i); // Если делится без остатка, выводим это число
        //     }
        //     i = i + 1; // Увеличиваем i на 1 для перехода к следующему числу
        // }

        // Задача: Пользователь вводит 2 числа a и b. Распечатать все числа от a до b
        // Пример: если введено 20 и 30, нужно вывести числа 20, 21, 22, ..., 30

        // Читаем первое число от пользователя и сохраняем в переменную a
        int a = sc.nextInt();
        // Читаем второе число от пользователя и сохраняем в переменную b
        int b = sc.nextInt();

        // Создаем переменную i для перебора чисел и инициализируем её значением a
        // (начинаем с первого числа диапазона)
        int i = a;

        // Цикл while выполняется пока i меньше или равно b (включая границу b)
        while (i <= b) {
            // Выводим текущее значение i (очередное число в диапазоне от a до b)
            System.out.println(i);
            // Увеличиваем i на 1, чтобы перейти к следующему числу в диапазоне
            i = i + 1;
        }

    }
}
